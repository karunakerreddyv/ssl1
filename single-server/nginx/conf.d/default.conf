# =============================================================================
# Pravaha Platform - Empty default.conf
# =============================================================================
# This empty file overrides nginx:alpine's built-in default.conf to prevent
# conflicts with our custom configuration. The default nginx config includes
# a server block with server_name=localhost that would intercept health check
# requests before they reach our custom default server block.
#
# Health checks are handled by the default server in nginx.conf
# =============================================================================
